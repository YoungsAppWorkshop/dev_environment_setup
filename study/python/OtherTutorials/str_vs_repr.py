#!/usr/bin/env python3
# Python Tutorials
# str() vs repr()
# The goal of __str__ is to be readable
# The goal of __repr__ is to be unambiguous

a = [1, 2, 3, 4]
b = 'sample string'

print(str(a))
print(repr(a))

print(str(b))
print(repr(b))
